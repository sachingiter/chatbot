import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CurrenthistoryPage } from './currenthistory';

@NgModule({
  declarations: [
    CurrenthistoryPage,
  ],
  imports: [
    IonicPageModule.forChild(CurrenthistoryPage),
  ],
})
export class CurrenthistoryPageModule {}
